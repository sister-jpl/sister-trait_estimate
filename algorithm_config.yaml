# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algorithm_name: sister-trait_estimate
algorithm_version: 1.0.0
repository_url: https://github.com/EnSpec/sister-trait_estimate.git
docker_container_url: mas.dit.maap-project.org/root/maap-workspaces/base_images/vanilla:dit

# fill out these fields
# explain what this algorithm does
algorithm_description: Estimate canopy chlorophyll, nitrogen and leaf mass per area area.
# path to the build script for installing and building the algorithm in the docker image
build_command: sister-trait_estimate/install.sh
# path to the wrapper script for running the algorithm
run_command: sister-trait_estimate/pge_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:

  file:
    - name: reflectance_dataset
      description:
      required: True
    - name: frcov_dataset
      description:
      required: True
  config:
    - name: veg_cover
      data_type: number
      default: '0.5'
      description: Minimum vegetation cover threshold
    - name: crid
      data_type:
      default: '000'
      description: Composite release identifier
    - name: experimental
      default: "True"
